<h1>FtRequest edit</h1>
<style>
input { width: 280px; }
#ft_homebundle_ftrequesttype_report_summary {width: 1100px;  }
#ft_homebundle_ftrequesttype_notes {width: 1100px;  }
</style>
<form action="{{ path('ft_request_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('ft_request') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('ft_request_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
